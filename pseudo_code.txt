1. a method that returns the sum of two integers

Given two integers
get an integer from user and set it to a variable
get another integer from user and set it to another variable
add the two variables together and set it to a variable named result
print result


START
GET first number
GET second number
SET the result of adding the two numbers
PRINT result

2.a method that takes an array of strings, and returns a string that is all those strings concatenated together

given a collection of strings named "strings"
takes each string in the strings and adds each string together with spaces in between, into a new variable
returns that variable

START

WHILE the array has a collection of strings, add each string into a  variable, with spaces inbetween


3. a method that takes an array of integers, and returns a new array with every other element

Given an array, select each item whose indexes are an even number, and push them onto a new array.